// project roles
export const Roles = ['DEVELOPER', 'LAB']

// define sections, and specify which roles can access them
export const RoleSections = {
	example: ['DEVELOPER'],
}

// define home page for each role
export const RoleHome = {
	DEVELOPER: '/example',
}
