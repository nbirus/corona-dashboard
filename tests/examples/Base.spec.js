/*
// setup
import Vuetify from 'vuetify'
import { mount } from '@vue/test-utils'
import ComponentSetup from '@/../tests/helpers/ComponentSetup'

// create component
import Component from '@/views/authentication/ExpirationGaurd'
const localVue = ComponentSetup()

describe('expiration', () => {

  // mock vuetify
  let vuetify
  beforeEach(() => { vuetify = new Vuetify() })

  it('default', () => {
    const wrapper = mount(Component, { localVue, vuetify })
    expect(true).toBe(true)
  })

})
*/